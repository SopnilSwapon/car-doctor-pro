import { Box, Typography } from '@mui/material';
import React from 'react';

const page = () => {
    return (
        <Box sx={{minHeight: 'calc(100vh - 303px)', mt: '80px'}}>
            <Typography variant='h5' textAlign='center'>
                About page coming soon!!
            </Typography>
        </Box>
    );
};

export default page;